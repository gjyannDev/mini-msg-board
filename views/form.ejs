<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body
    class="bg-[var(--base-white)] text-[var(--base-black)] font-spectral-regular"
  >
    <%- include('partials/header') %>

    <main class="container flex flex-col gap-14 mx-auto px-15 my-8">
      <div class="flex flex-col gap-6">
        <button type="button" class="w-fit cursor-pointer text-lg">
          <a href="/"> ← Back</a>
        </button>
        <h1 class="font-extrabold text-4xl max-w-lg leading-[1.3]">
          Create Message to share :)
        </h1>
      </div>
      <form method="POST" action="/new" class="flex flex-col gap-8">
        <div class="flex flex-col gap-2">
          <label for="" class="font-bold text-lg">Your name</label>
          <input
            type="text"
            placeholder="John Doe"
            name="name"
            class="w-full max-w-lg h-12 rounded-sm shadow-md placeholder:text-[var(--base-black)] focus:ring-2 border border-gray-300 dark:border-gray-700 lg:max-w-sm lg:h-11 px-4"
            required
          />
        </div>
        <div class="flex flex-col gap-2">
          <label for="message" class="font-bold text-lg">Your message</label>
          <textarea
            id="message"
            placeholder="Write your message here..."
            rows="4"
            name="message"
            class="w-full max-w-lg rounded-sm shadow-md placeholder:text-[var(--base-black)] focus:ring-2 border border-gray-300 dark:border-gray-700 lg:max-w-sm px-4 py-3 resize-none"
            required
          ></textarea>
        </div>
        <button
          class="w-fit font-semibold bg-[var(--primary-clr)] px-5 py-3 rounded-sm cursor-pointer"
        >
          Send Message
        </button>
      </form>
    </main>
  </body>
</html>
