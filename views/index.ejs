<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body
    class="bg-[var(--base-white)] text-[var(--base-black)] font-spectral-regular"
  >
    <%- include('partials/header') %>

    <main class="container flex flex-col gap-24 mx-auto px-15">
      <div class="flex flex-col gap-6 mt-14">
        <h1 class="font-extrabold text-3xl max-w-sm leading-[1.3]">
          Hi there, Leave a message for everyone.
        </h1>
        <button
          class="w-fit font-semibold bg-[var(--primary-clr)] px-4 py-2 rounded-sm cursor-pointer"
        >
          <a href="/new">Add Message</a>
        </button>
      </div>

      <div class="mb-16">
        <%- include('partials/noteCard.ejs', {messages: messages}) %>
      </div>
    </main>
  </body>
</html>
